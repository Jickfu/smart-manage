# 数据权限设计

## 当前状态

功能权限和对象状态授权已经生效，组织级、本人级和自定义组织范围尚未启用。业务代码在全局数据权限落地前不得宣称列表、详情或命令已经完成行级隔离。

当前先统一数据归属字段：具有保存、提交、审核等生命周期的业务单据继承 `BaseBillEntity`，使用 `org_id` 表达单据所属组织。`applicant_id`、采购员、经办人、负责人等字段不是所有单据共有的语义，只能由具体聚合按业务需要声明。

## 权限分层

功能权限回答“能否执行某项能力”，继续使用现有权限码。数据权限回答“可以访问哪些业务对象”，不得用新增功能权限码模拟数据范围。

计划支持以下标准范围：

```text
ALL                 全部数据
ORG_AND_CHILDREN    当前组织及下级组织
ORG                 当前组织
SELF                本人相关数据
CUSTOM_ORGS         指定组织集合
```

同一用户在当前组织下拥有多个角色时，授权范围按并集合并；`administrator` 固定为 `ALL`。切换当前组织后必须重新解析数据范围，不能复用其他组织上下文中的结果。

## 后端边界

未来由系统公共能力提供 `DataScopeService.resolve(resourceType, action)`，返回包含范围类型、组织 ID 集合和当前用户 ID 的不可变上下文。

业务查询必须显式把该上下文传给 Mapper，由可读 SQL 或类型安全 Wrapper 生成条件。初始版本禁止通过 ThreadLocal SQL 拦截器自动向任意查询拼接 `org_id`，因为审批人、申请人、协作人和所属组织并不总是同一语义，自动改写也难以审计复杂联表和统计查询。

详情、修改、删除、提交和附件访问不能只依赖列表过滤。对象级策略至少组合校验：

```text
功能权限
+ 数据范围
+ org_id 等对象归属
+ 具体业务角色字段
+ 单据状态
```

对象授权查询只加载判断所需的最小字段，不加载完整聚合。

## 落地顺序

1. 设计角色数据范围配置表和自定义组织关系迁移。
2. 实现纯逻辑的范围合并与组织树解析，并覆盖自动化测试。
3. 以采购申请作为首个试点，覆盖列表、详情、修改、删除和附件访问。
4. 验证查询性能和索引后，再扩展到其他业务聚合。
5. 显式模式稳定后，才允许为结构完全一致的简单主数据提供受控查询辅助器。

数据权限正式实施前，采购申请保留 `org_id` 和 `applicant_id` 作为未来策略所需的事实字段，但暂不据此过滤查询。
