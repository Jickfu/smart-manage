name: Quality Gate

on:
  push:
  pull_request:

jobs:
  conventions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify module conventions
        shell: pwsh
        run: ./scripts/verify-module-conventions.ps1

  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Test backend
        working-directory: smart-manage-api
        run: mvn --batch-mode --no-transfer-progress test

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.17.0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: pnpm
          cache-dependency-path: smart-manage-web/pnpm-lock.yaml
      - name: Install dependencies
        working-directory: smart-manage-web
        run: pnpm install --frozen-lockfile
      - name: Lint
        working-directory: smart-manage-web
        run: pnpm lint
      - name: Check formatting
        working-directory: smart-manage-web
        run: pnpm format:check
      - name: Test
        working-directory: smart-manage-web
        run: pnpm test
      - name: Build
        working-directory: smart-manage-web
        run: pnpm build
      - name: Verify generated registry is committed
        run: git diff --exit-code -- smart-manage-web/src/domain/common/registry/registry.gen.ts

  migrations:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Verify migrations from an empty database
        shell: pwsh
        run: ./db/verify-baseline.ps1 -PsqlPath /usr/bin/psql
